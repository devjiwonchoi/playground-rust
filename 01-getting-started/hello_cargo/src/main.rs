fn main() {
    println!("Hello, world!");
    // if code's modified, cargo run builds and runs the code
    println!("I'm a Rustacean!");
}

// create project - cargo new <project-name>
// build project - cargo build
// build & run project - cargo run
// check project - cargo check
// saves file to target/debug
// --release flag for production -> saves file to target/release